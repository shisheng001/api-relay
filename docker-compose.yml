version: '3.8'

services:
  api-relay:
    build: .
    ports:
      - "8000:8000"
    environment:
      - TZ=Asia/Shanghai
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    restart: unless-stopped
    networks:
      - api-network

networks:
  api-network:
    driver: bridge
